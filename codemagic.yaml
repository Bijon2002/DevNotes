workflows:
  # 1. Fast Debug Build (Zero environment variables required)
  android-debug:
    name: Android Debug Build
    max_build_duration: 30
    environment:
      node: 22
      java: 21
    scripts:
      - name: Install npm dependencies
        script: |
          npm install
      - name: Build Web App
        script: |
          npm run build
      - name: Sync Capacitor Android
        script: |
          npx cap sync android
      - name: Build Debug APK
        script: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug
    artifacts:
      - android/app/build/outputs/**/*.apk

  # 2. Production Signed Release Build (Requires Codemagic 'android_signing' group)
  android-release:
    name: Android Release Signed Build
    max_build_duration: 30
    environment:
      groups:
        - android_signing
      node: 22
      java: 21
    scripts:
      - name: Install npm dependencies
        script: |
          npm install
      - name: Build Web App
        script: |
          npm run build
      - name: Sync Capacitor Android
        script: |
          npx cap sync android
      - name: Decode Keystore from Environment Variable
        script: |
          echo "$CM_KEYSTORE" | base64 --decode > /tmp/release.keystore
      - name: Build Signed Release APK
        script: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease \
            -Pandroid.injected.signing.store.file=/tmp/release.keystore \
            -Pandroid.injected.signing.store.password=$CM_KEYSTORE_PASSWORD \
            -Pandroid.injected.signing.key.alias=$CM_KEY_ALIAS \
            -Pandroid.injected.signing.key.password=$CM_KEY_PASSWORD
    artifacts:
      - android/app/build/outputs/**/*.apk
